// ReviewActions: approve / reject / comment action buttons for a PR
// Calls POST /api/pullrequests/:id/approve|reject|comment
// Disabled when PR state does not allow the action

function ReviewActions() {
  return <div>ReviewActions - placeholder</div>;
}

export default ReviewActions;
