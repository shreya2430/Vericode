// PRForm: controlled form for submitting a new PR
// Fields: title, description, language (Java/Python/JS), code (textarea)
// On submit: calls prService.submitPR()

function PRForm() {
  return <form>PRForm - placeholder</form>;
}

export default PRForm;
